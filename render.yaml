services:
  - type: web
    name: taskflow-pro
    runtime: static
    buildCommand: npm install --include=optional && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: NODE_VERSION
        value: 20
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
